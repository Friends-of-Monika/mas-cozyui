// .cozy config versioning/migration lives in the shared theme-builder package;
// re-exported here so existing `#lib/theme/migrate` imports keep resolving.
export * from "@cozyui/theme-builder/migrate";
