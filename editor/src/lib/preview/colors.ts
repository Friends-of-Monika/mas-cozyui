// The color logic lives in the shared theme-builder package (used by both the
// editor and the Node theme builder). Re-exported here so the many existing
// `#lib/preview/colors` imports keep resolving.
export * from "@cozyui/theme-builder/colors";
